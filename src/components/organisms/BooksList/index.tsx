import { Grid, Typography } from "@mui/material";
import React from "react";

const BooksList = ({ data }) => {
  return (
    <>
      <Typography>Book List Heading</Typography>
      <Grid>
        {/* display flex with flex direction row maps book cards with data */}
      </Grid>
    </>
  );
};

export default BooksList;
